function updateTaskList() {
    var taskListStr = "";
    //update the button ids to make them match the array
    //display each item in the array 
    for (i = 0; i < taskList.length - 1; i++) {
        document.getElementById(taskListStr)
    };
}